# User Preferences & Teaching Notes

- **Stack**: React, Next.js App Router, TypeScript.
- **State / Data Fetching**: TanStack Query (React Query).
- **Current App Complexity**: Low to Medium (mostly many components and fetch files organized loosely by domain).
- **Core Learning Interests**:
  - Understanding Coupling vs Cohesion in frontend.
  - How to structure Next.js App Router with FSD (`src/app` as thin routes, business logic in `src/entities`, `src/features`, `src/widgets`).
  - How to integrate TanStack Query hooks, query keys, and mutations cleanly into FSD `model` and `api` segments.
  - Public API (`index.ts`) patterns and automated linting (Steiger).
  - Studying real-world open-source codebases (`realworld-react-fsd`).
